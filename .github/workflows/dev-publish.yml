# Auto-generated by sync-dev-publish.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Dev Publish

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  dev-publish:
    uses: greenticai/.github/.github/workflows/dev-publish.yml@main
    with:
      crates: "greentic-cap-types greentic-cap-profile greentic-cap-resolver greentic-cap-schema greentic-cap-core greentic-cap"
    secrets: inherit
